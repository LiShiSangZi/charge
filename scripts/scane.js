'use strict';

/**
 * 扫描数据，如果现有数据与计费数据不符，做简单的数据纠正。
 */
const ctx = require('./context');
const runner = async() => {
  // Fetch all users:
  const users = await ctx.service.keystone.fetchUsers();
  return token;
}

runner().then(res => {

});